# docs/openapi.yaml
openapi: 3.0.0
info:
  title: Invoice Service API
  version: 1.0.0
paths:
  /health:
    get:
      summary: Health check
  /invoices:
    get:
      summary: List all invoices
    post:
      summary: Create new invoice